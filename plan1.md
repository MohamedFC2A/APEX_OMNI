# خطة نظام Apex Omni وإصلاح الأخطاء الحرجة

## المشاكل الحرجة التي يجب معالجتها أولاً:

### 1. أخطاء DOM الحرجة (Nested Buttons)

- **الموقع**: `ModePopover.tsx` و `NexusChat.tsx`
- **المشكلة**: `<button>` داخل `<button>` يسبب أخطاء hydration
- **الحل العاجل**: فحص السطر 63 في `ModePopover.tsx` والسطر 577 في `NexusChat.tsx`

### 2. مشاكل Hydration و SSR

- **المشكلة**: عدم تطابق بين الخادم والعميل
- **الحل**: تنفيذ `useHasMounted` hook

## خطة التنفيذ التفصيلية:

### المرحلة 1: إصلاح الأخطاء الحرجة (الأولوية القصوى)

1. إصلاح nested buttons في `ModePopover.tsx`
2. إصلاح nested buttons في `NexusChat.tsx`
3. تنفيذ `useHasMounted` hook لسلامة SSR

### المرحلة 2: إعادة هيكلة نظام المودات

1. **⚡ FLASH MODE**: `xiaomi/mimo-v2-flash:free` (أسرع استجابة)
2. **🧠 DEEP THINKING MODE**: تحسين النظام الحالي
3. **⚛️ NEXUS APEX OMNI**: نظام Swarm جديد

### المرحلة 3: بناء نظام Apex Omni Swarm

1. تكوين Swarm من 4 نماذج متخصصة:

- `nex-agi/deepseek-v3.1-nex-n1:free` (المجمع النهائي)
- `kwaipilot/kat-coder-pro:free` (متخصص البرمجة)
- `openai/gpt-oss-20b:free` (المنطق)
- `allenai/olmo-3.1-32b-think:free` (التفكير العميق)

2. آلية التجميع النهائي:

- `deepseek-v3.1-nex-n1` يجمع كل المخرجات
- ينتج "الحقيقة المطلقة" بتنسيق Markdown احترافي

### المرحلة 4: نظام متعدد اللغات

1. إضافة تبديل AR/EN في الهيدر
2. دعم كامل لـ RTL للعربية
3. استخدام خط Cairo للعربية
4. تبديل سلس بدون إعادة تحميل

### المرحلة 5: NEXUS PLAN LIVE

1. إعادة تسمية "Thinking Live" إلى "NEXUS PLAN LIVE"
2. تصميم واجهة "Blueprint" style
3. عرض خطة التنفيذ ووقت التفكير لكل نموذج
4. إضافة telemetry للعرض

### المرحلة 6: AI Typing & Telemetry

1. إضافة تأثيرات typing مستقبلية
2. عرض tags توضح النموذج المساهم
3. تحسين تجربة المستخدم

### المرحلة 7: إصلاح Input & Persistence

1. إصلاح bug الـ 1-character
2. ربط history بالـ swarm
3. ضمان وعي الـ AI الكامل بالمحادثات السابقة

### المرحلة 8: تحسين الأداء والواجهة

1. إزالة "Overkill Red Neon"
2. استخدام "Clean Obsidian Glass"
3. ألوان: "Neon Cyan" للحالات الجاهزة، "Neon Emerald" للحالات النشطة
4. دعم كامل للـ keyboard (Enter to Send)

## الملفات الرئيسية التي تحتاج تعديل:

1. `web/src/components/ModePopover.tsx` - إصلاح nested buttons
2. `web/src/components/NexusChat.tsx` - إصلاح nested buttons وتحسينات
3. `web/src/hooks/useHasMounted.ts` - hook جديد
4. `web/src/app/api/nexus/route.ts` - تحديث نظام المودات
5. `web/src/lib/nexusMeta.ts` - تحديث تعريفات المودات
6. `web/src/components/NexusChat.tsx` - إضافة تبديل اللغة
7. `web/src/app/globals.css` - إضافة دعم RTL وخطوط

## الاعتبارات التقنية: